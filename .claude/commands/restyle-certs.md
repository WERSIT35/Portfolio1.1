Restyle certification UI on both:
- http://localhost:4200/
- http://localhost:4200/allcert

Scope and rules:
- Keep all certification text/content unchanged.
- Make minimal, focused edits only.
- Reuse existing SCSS tokens/mixins.
- No new dependencies.
- Ensure responsive behavior (mobile/tablet/desktop).
- Preserve accessibility (contrast, readable type, focus states).
- If shared card styling is affected, update `src/styles/_cards.scss`.

Output format:
1. Short plan (max 5 bullets)
2. Changed files
3. Exact diffs/snippets
4. Verification checklist for both URLs
