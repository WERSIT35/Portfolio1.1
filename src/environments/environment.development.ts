export const environment = {
  production: false,
  /**
   * Set to `true` when running `npm run backend:dev` alongside `ng serve` to
   * exercise the live API. Leave `false` to use the static snapshot.
   */
  useApi: false,
  apiBase: 'http://localhost:4300/api',
};
